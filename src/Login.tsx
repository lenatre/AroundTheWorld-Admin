import{FormEvent,useState}from'react';import{signInWithEmailAndPassword}from'firebase/auth';import{Compass,LockKeyhole,Mail}from'lucide-react';import{auth}from'./firebase';
export default function Login(){const[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);try{setBusy(true);setError('');await signInWithEmailAndPassword(auth,String(d.get('email')).trim(),String(d.get('password')))}catch{setError('Email or password is incorrect.')}finally{setBusy(false)}}return <main className="login"><section><div className="admin-mark"><Compass/></div><small>JOURNEY CONTROL</small><h1>Welcome back</h1><p>Sign in to manage student journeys.</p><form onSubmit={submit}><label>EMAIL<div><Mail/><input type="email" name="email" required autoComplete="email" placeholder="you@example.com"/></div></label><label>PASSWORD<div><LockKeyhole/><input type="password" name="password" required autoComplete="current-password" placeholder="Your password"/></div></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in to Admin'}</button></form></section></main>}
